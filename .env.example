APP_NAME=Order Management API
DEBUG=false
POSTGRES_DB=orders
POSTGRES_USER=orders
POSTGRES_PASSWORD=local-development-only
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# Generate before deploying: python -c 'import secrets; print(secrets.token_urlsafe(48))'
JWT_SECRET=local-development-only-replace-before-deploying
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
CURRENCY_API_URL=https://api.frankfurter.dev/v2
CURRENCY_TIMEOUT_SECONDS=5
API_PORT=8000
